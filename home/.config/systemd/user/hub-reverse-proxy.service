[Unit]
Description=Hub Reverse Proxy (routes hub-api + TermHive)
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/hub
ExecStart=python3 reverse-proxy.py
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
