[Service]
# Keep automatic recall compact and AI-readable. The lifecycle helper still
# stores full audit data and can deepen memory lookups when a task needs it.
Environment=OPENCODE_LIFECYCLE_CONTEXT_CHARS=1800
