[Service]
# OpenCode starts MCP helper subprocesses. The host user limit was 2048 while
# the user slice could sit near that limit, causing watchdog fork failures and
# making task recovery brittle.
LimitNPROC=8192
