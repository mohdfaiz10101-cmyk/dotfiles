[Service]
# OpenCode sessions can temporarily grow when large histories or MCP context are
# active. Keep a hard ceiling, but do not throttle at 4G while the host still
# has plenty of memory.
MemoryHigh=10G
MemoryMax=16G
