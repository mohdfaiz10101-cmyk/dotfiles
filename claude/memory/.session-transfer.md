## 上下文转移摘要
**来源 Session**: Sisyphus — GLM 工作台前端验证
**时间**: 2026-05-19 17:42

### 原始需求
创建 GLMWorkbenchPanel 组件（3列仪表板），集成到 agi-control-plane 前端 (:3000)。

### 已完成
- [OK] 创建 GLMWorkbenchPanel.tsx → 3列布局（竞品分析/代码库地图/Aider审查）+ JobsTable + Badge模型标签 + 30s自动刷新
- [OK] 注册到 page.tsx PANEL_MAP：`"glm-workbench": GLMWorkbenchPanel`
- [OK] Sidebar 聊天组已包含 GLM 工作台入口
- [OK] 页面渲染正常 → 截图在 ~/.playwright-mcp/glm-workbench-final.png

### 踩坑（必须知道）
- **page.tsx 曾被人回滚**：文件从112行退回到56行旧版，所有移动端适配+GLM路由丢失。已手动恢复完整112行版。
- **dev server 是生产模式**：`agi-frontend.service` 用 `npx next start`（非 `next dev`），改源码后必须 `bun run build` 重建 `.next` 才能生效。**不要试图杀了进程起 dev mode，service 会自动重启。**
- **端口 3000 的进程**：`systemctl --user status agi-frontend.service` 即可查看/重启。

### 未完成
- [ ] Hub API `/api/glm/workbench`（端口 9800）当前只返回 `{models, note}`，缺少 `competitive`/`codebase`/`aider`/`jobs` 字段 → 三列显示「暂无数据」
- [ ] 需要后端补充数据源（竞品分析结果、代码库地图坐标、Aider审查输出、Jobs列表）

### 关键文件
- `/mnt/ai/apps/agi-control-plane/frontend/app/page.tsx` — PANEL_MAP (112行，已恢复)
- `/mnt/ai/apps/agi-control-plane/frontend/app/components/glm/GLMWorkbenchPanel.tsx` — 完整组件(203行)
- `curl http://localhost:9800/api/glm/workbench` — 当前返回

### 下一步
1. 在 Hub API（9800）补充 `/api/glm/workbench` 端点数据
2. 验证前端三列+JobsTable正常加载真实数据
3. 修复 React hydration error (#418) — 来自 CopilotSidebar，不影响功能但控制台报错