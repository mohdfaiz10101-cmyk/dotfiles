---
name: 2026-05-26 会话变更记录
description: ttyd端口修复、opencode配置、alias、Telegram群组、FRP端口映射澄清
type: project
---

## 关键修复（2026-05-26/27）

### FRP 端口映射澄清（重要）
- **19890 → 本地 8080 → `openclaw` tmux session**（opencode工作区窗口）
- **18090 → 本地 8088 → `sisy` tmux session**（sisyphus agent）
- 之前 MEMORY.md 误记为 "opencode:19890"，实际是 openclaw 工作区

### Shell Alias
- `oc` = `tmux attach-session -t openclaw`（同 19890 端口完全一致）
- `overcode` = `opencode`
- 写入: `/etc/nixos/modules/users.nix` + `~/.zshrc`

### ttyd 端口冲突根治
- 所有 ttyd-*.service 的 ExecStartPre 从 `ss -tlnp | grep && exit 1` 改为 `fuser -k <port>/tcp`
- 旧方案：检测到占用就拒绝启动 → 新方案：杀掉旧进程再启动
- 涉及服务：ttyd-opencode / ttyd-8080 / ttyd-aider / ttyd-cct / ttyd-claudep / ttyd-claude / ttyd-foc / ttyd-gemini / ttyd-macg / ttyd-overcode / ttyd-overtab / ttyd-ulwh

### opencode 配置
- `tui` block 从 opencode.json 迁移到 tui.json（1.15.7 deprecation warning）
- theme 统一 `catppuccin`，code_blocks/tool_output/hide_thinking 在 tui.json

### opencode-tmux-wrap
- 启动目录改为 `~/.openclaw/workspace`
- 启动命令加 `--continue` 续接上次 session

### Telethon 用户账号
- api_id: 30310490 / api_hash: 440432536b811c895fa4c1a31cf01fac
- 电话: +8615658509739
- Session 文件: `~/.local/share/telegram/sisyphus_userbot.session`
- Session 格式迁移完成（移除 tmp_auth_key，兼容 telethon 1.42.0）
- 建群脚本: `~/.local/bin/tg-auth.py`（request→login→create 三步）

**Why:** FRP端口映射一直有误记，导致oc alias接错session。
**How to apply:** 访问opencode工作区用 `oc` 或 http://frp:19890，sisyphus用 http://frp:18090
