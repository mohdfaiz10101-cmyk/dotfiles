[Service]
# 等待 /mnt/ai 挂载后再启动（用户级 systemd 无法依赖系统级 mount unit）
ExecStartPre=/bin/sh -c 'for i in $(seq 1 30); do mountpoint -q /mnt/ai && exit 0; sleep 2; done; exit 1'
TimeoutStopSec=3
KillSignal=SIGQUIT
